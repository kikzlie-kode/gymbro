rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the Cloud Functions API (verified via LINE ID token),
    // which uses the Admin SDK and bypasses these rules. Client-side direct access is blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
